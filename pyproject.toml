[project]
name = "multiply"
version = "0.1.0"
description = "Player/club valuation tracking with auto-notification for investors and fans"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
