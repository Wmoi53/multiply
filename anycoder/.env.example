# Copy to .env for local backend runs (`docker run --env-file .env ...`),
# or set as a Space secret when deploying. Not required if every user
# supplies their own token via the frontend's dev-mode field instead.
HF_TOKEN=hf_xxx
